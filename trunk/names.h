// defines global constants used in place of ids

// defines constant for build grist
const unsigned int BUILD_GRIST = 0;

// defines constants for player special grists
const unsigned int PLAYER_GRISTS[16];
for ( unsigned int i = 0; i < 16; i ++ )
{
   PLAYER_GRISTS[i] = i + 1;
}

// defines constant names for players
const unsigned int PLAYERS[16];
for ( unsigned int i = 0; i < 16; i ++ )
{
   PLAYERS[i] = i;
}
